let names=['kuber','abhi','karan'];
// names='ks'//cannot change the array type also they are static

names.push('riya')
// names.push(3) // number is assignable

let number=[10,20,30,40];
number.push(25);
// number.push('silpa');
// number[1]='shaun'

let mixed=[1,2,'karan',5,'kuber']
mixed.push('sakshi')
mixed[0]=3 //so here it can be used as both string and number
//moreover the concept is the datatype we will pass it will take that types in solution


//objects:

let data={
  name:'kuber',
  lname:'seth',
  age:55
}
data.age=55
data.name='priya'
// data.lname=50-->here also same thing happens

    